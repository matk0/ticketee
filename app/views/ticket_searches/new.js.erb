$("#tickets").replaceWith('<%= j render "projects/tickets_table" %>');
$(function() {
  $("#tickets").tablesorter({
    headers: {
      0: { sorter: false },
      4: { sorter: false }
    }
  });
});
